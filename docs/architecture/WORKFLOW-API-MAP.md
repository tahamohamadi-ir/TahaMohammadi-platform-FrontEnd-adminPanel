# Workflow API Map

This map is an implementation gate, not an endpoint inventory. Exact methods and schemas come only from accepted authenticated admin OpenAPI artifacts (49 paths / 106 operations, addendum 2026-09-04b).

**ADMIN-250 status (2026-09-04):** every mutation shipped by this client is mapped below to the server-side test that proves its guard, plus the client-side test that proves the UI surfaces the failure honestly. Coverage column: ✅ dedicated server test exists · ⬜ gap tracked by **BACKEND-190** (server permission-matrix tests) — implementing a mutation without a server guard test is blocked by this map.

Shared guard model (all admin ops): staff session (`401 AUTH_REQUIRED` anonymous → `403 FORBIDDEN` non-staff) + verified OTP (`403 OTP_REQUIRED`) + same-origin CSRF on mutations (`403 CSRF_FAILED`).

| #   | Workflow          | Mutations (method + path)                                                                                                              | Client surface                                                 | Server guard evidence                                                                                                                                                                                                                                                                                                                                                            | Client failure-surface evidence                                                                                                                                 | Coverage                                            |
| --- | ----------------- | -------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| 1   | Session           | `POST /auth/login` · `POST /auth/logout` · `GET /auth/csrf` · `GET /auth/me`                                                           | `SignInPage`, `AuthProvider`, `lib/api/auth`                   | `tests/test_admin_api_auth.py` (login requires OTP when enrolled; auth-required/forbidden/OTP-required/CSRF matrix) · `tests/test_admin_mfa_api.py`                                                                                                                                                                                                                              | `lib/api/auth.test.ts`, `lib/api/errors.test.ts`, `app/router.test.tsx`                                                                                         | ✅                                                  |
| 2 | Content lifecycle | `POST /content/{entity}` · `PUT /content/{entity}/{id}` · `POST …/transition` · `POST …/bulk-archive` | `ContentEditPage`, `ContentListPage`, `hooks/useContent` | `tests/test_admin_content_write.py` (`test_write_requires_otp`, `test_update_conflict_409`) · `tests/test_admin_bulk_archive.py` (`test_bulk_archive_disabled_returns_404`, success + audit) · `tests/test_admin_approvals.py` (`test_publish_blocked_without_owner_approval`, `test_publish_allowed_once_owner_triple_cleared`, `test_publish_allowed_without_seed_provenance`) · `tests/test_admin_permission_matrix.py` (non-staff 403 for create/update/transition/bulk-archive) | `pages/ContentEditPage.test.tsx` (validation block, conflict reload, schedule dialog), `pages/ContentListPage.test.tsx` (bulk flag off/on) | ✅ |
| 3 | Content revisions | `GET/POST /content/{entity}/{id}/revisions` · `POST …/revisions/{rid}/restore` | `ContentEditPage` History section, `hooks/useContentRevisions` | `tests/test_admin_revisions_schedule.py` (revisions + scheduled publish matrix, OTP/CSRF) · `tests/test_admin_permission_matrix.py` (non-staff 403 for revisions create/restore + reads) | `pages/ContentEditPage.test.tsx` (history list, snapshot, confirm-guarded restore) | ✅ |
| 4 | Publication gate | gate runs inside `transition to=published` **and** `publish_scheduled_content` | `ContentEditPage` (publish disabled + notice) | `tests/test_admin_approvals.py` (blocked / cleared / no-provenance / scheduler honors gate) | `pages/ContentEditPage.test.tsx` (`blocks publish while owner approval is missing`) | ✅ |
| 5 | Media | `POST /media` (multipart) · `PUT /media/{id}` · `DELETE /media/{id}` | `MediaPage`, `hooks/useMedia` | `tests/test_admin_media_api.py` (`test_list_requires_otp`, `test_delete_media_requires_auth/otp/csrf`, `test_update_conflict_409`) · `tests/test_admin_media_ext.py` (presentation PATCH + licenses guards) · `tests/test_admin_permission_matrix.py` (non-staff 403 for upload/update/delete/list/licenses/orphans) | `pages/MediaPage.test.tsx` (upload, confirm delete, in-use 409 verbatim) | ✅ |
| 6   | Home modules      | `GET/PUT /home-modules/{locale}` · `POST …/validate`                                                                                   | `HomePage`, `hooks/useHome`                                    | `tests/test_admin_home_api.py` (`test_anonymous_get_is_401_auth_required`, `test_non_staff_user_is_403_forbidden`, `test_staff_without_verified_otp_is_403_otp_required`, `test_put_without_csrf_token_is_403_csrf_failed`, `test_put_stale_if_match_is_409_stale_revision`)                                                                                                     | `pages/HomePage.test.tsx` (validate, save with If-Match, stale reload notice)                                                                                   | ✅                                                  |
| 7   | Timeline          | `GET/POST /timeline/{locale}` · `POST …/reorder` · `PATCH/DELETE …/{id}`                                                               | `TimelinePage`, `hooks/useTimeline`                            | `tests/test_admin_timeline_api.py` (anonymous 401, non-staff 403, OTP 403, CSRF 403, `test_patch_stale_if_match_is_409_stale_revision`, `test_delete_stale_if_match_is_409`)                                                                                                                                                                                                     | `pages/TimelinePage.test.tsx` (create, reorder permutation, If-Match patch, confirm delete)                                                                     | ✅                                                  |
| 8   | Research graph    | `GET/POST /graph/versions` · `GET …/payload` (detail) · `PUT …/payload` · `POST …/activate` · `GET /graph/validation/{id}`             | `GraphPage`, `GraphEditPage`, `hooks/useGraph`                 | `tests/test_admin_graph_adminside.py` (anonymous 401, non-staff 403, OTP 403, CSRF 403, `test_put_stale_if_match_409`) · `tests/test_admin_graph_validate.py`                                                                                                                                                                                                                    | `pages/GraphPage.test.tsx` (draft create, confirm activate, active-row immutability), `pages/GraphEditPage.test.tsx` (payload save If-Match, validation report) | ✅                                                  |
| 9 | Site settings | `GET/PUT /site` | `SettingsPage`, `hooks/useSiteSettings` | `tests/test_admin_siteconfig_api.py` (`test_settings_put_stale_if_match_409`) · `tests/test_admin_seed_policy.py` (`test_seed_policy_is_not_writable_via_update`, `test_admin_site_response_carries_seed_policy`) · `tests/test_admin_permission_matrix.py` (non-staff 403 for GET/PUT) | `pages/SettingsPage.test.tsx` (client limits, conflict reload, seed policy panel) | ✅ |
| 10 | Approval queue | `GET /approval-queue` (read-only by design — no mutation endpoint exists; approving is an owner decision recorded in the seed package) | `ApprovalQueuePage`, `hooks/useApprovals` | `tests/test_admin_approvals.py` (`test_approval_queue_requires_admin_otp`, counts/default-filter) · `tests/test_admin_permission_matrix.py` (non-staff 403 + staff-without-OTP 403) | `pages/ApprovalQueuePage.test.tsx` (rows + counts, read-only assertion, state filter) | ✅ |
| 11 | Preview share | `POST /content/{entity}/{id}/preview-link` (allowlist: landing/profile/article) | `ContentEditPage` Share-preview section, `lib/api/preview` | `tests/test_preview_share.py` (anonymous 401, staff success, unsupported 404) · `tests/test_admin_permission_matrix.py` (non-staff 403, OTP 403, CSRF 403, unsupported-entity 404 with full session) | `pages/ContentEditPage.test.tsx` (link + expiry shown; hidden for unsupported entities) | ✅ |
| 12 | Rebuild/health | Excluded from admin v1 (infrastructure-only operational control) | Not rendered in the admin client | N/A — no admin route, button, or client call is shipped | N/A | ✅ by exclusion |

## BACKEND-190 status: **closed (2026-09-04)**

`tests/test_admin_permission_matrix.py` pins the shared guard pair for every mutation and read the admin client ships: non-staff `403 FORBIDDEN` across content mutations + reads, media (upload/update/delete/list/licenses/orphans), site settings GET/PUT, and the approval queue; staff-without-OTP `403 OTP_REQUIRED` across surfaces; and the full preview-link guard matrix (anonymous 401 / non-staff 403 / OTP 403 / CSRF 403 / unsupported-entity 404).

The graph-editor concept in the tracked design authority guides layout and feedback hierarchy only. It does not create permission, status, endpoint, or content authority.
