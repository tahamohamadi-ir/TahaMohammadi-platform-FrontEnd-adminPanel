# Security Testing

Test CSRF, session expiry, MFA boundaries, permission denial, open redirects, reflected content, rich-content preview, upload constraints, sensitive data in logs/storage, cached unpublished data, cross-account leakage, and direct navigation to forbidden routes. Confirm the backend rejects unauthorized requests even when the UI is bypassed.
