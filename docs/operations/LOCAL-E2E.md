# Local E2E Environment

Use a disposable local backend configured with the test or E2E settings and seeded fixtures. Record backend commit, schema state, fixture version, admin commit, browser, and command in test evidence. Reset only the disposable database through a documented command; never reuse production credentials or data.
